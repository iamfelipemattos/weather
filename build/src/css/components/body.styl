require('./variables.styl')

*
  box-sizing border-box

body
  font font
  background linear-gradient(background-color, texture-color)
  width 100%
  height auto
  bottom 0px
  top 0px
  left 0;
  position absolute
